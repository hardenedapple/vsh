#!/bin/bash
# Nothing particularly difficult here -- just storing it for reference more than
# anything.  Want to dereference links so that the tarball created for an emacs
# package includes the relevant shell script and python files to get started.
tar -c -f vsh-1.0.tar --dereference vsh-1.0/
